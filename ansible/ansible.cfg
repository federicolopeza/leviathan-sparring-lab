[defaults]
host_key_checking   = False
remote_user         = root
private_key_file    = {{ lookup('env', 'SSH_PRIVATE_KEY_PATH') }}
retry_files_enabled = False
stdout_callback     = yaml
gathering           = smart
fact_caching        = memory
timeout             = 60

[ssh_connection]
pipelining          = True
ssh_args            = -o ControlMaster=auto -o ControlPersist=60s -o StrictHostKeyChecking=no

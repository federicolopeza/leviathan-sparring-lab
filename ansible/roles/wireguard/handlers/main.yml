---
# Role: wireguard — handlers

  - name: restart wg0
    ansible.builtin.systemd:
      name: wg-quick@wg0
      state: restarted

  - name: restart sshd
    ansible.builtin.systemd:
      name: sshd
      state: restarted

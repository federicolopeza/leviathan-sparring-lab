# Architecture — Melispy Inc. v3.0

## System Overview
TBD Phase 0 — full system diagram (Cloudflare → Traefik → api-gateway → 11 microservices → Postgres/Redis/MinIO/Vault).

## Service Catalog
TBD Phase 1 — each microservice: port, responsibilities, intentional vulns, DB schema.

## Data Flow
TBD Phase 2 — request lifecycle, audit trail, HMAC chain, mTLS internal mesh.
